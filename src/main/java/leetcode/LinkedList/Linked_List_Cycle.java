package leetcode.LinkedList;

/*判断一个链表里面是不是有环*/

public class Linked_List_Cycle {
    public boolean hasCycle(ListNode head) {
        ListNode fast = head;
        ListNode slow = head;

        if (head == null)
            return false;
    }
}
